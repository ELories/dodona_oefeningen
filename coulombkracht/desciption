
# Coulombkracht

Schrijf een functie `coulombkracht(q1, q2, r)` die de kracht tussen twee ladingen berekent volgens de formule:

$$
F = k \cdot \frac{|q_1 \cdot q_2|}{r^2}
$$

waarbij:
- `q1` en `q2` de ladingen zijn in Coulomb (float),
- `r` de afstand is in meter (float),
- `k = 8.988e9`.

### Voorbeeld

```python
>>> coulombkracht(1e-6, 2e-6, 0.05)
7.1904
